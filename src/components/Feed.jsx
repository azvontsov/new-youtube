import { useState, useEffect } from "react";
import { Box, Stack, Typography } from "@mui/material";

const Feed = () => (
  <Stack sx={{ flexDirection: { sx: "column", md: "row" } }}></Stack>
);

export default Feed;
